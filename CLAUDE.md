# 경희대산악부 홈페이지 (khuac.com)

Next.js (App Router) · Cloudflare Workers(OpenNext) · Supabase(Auth/Postgres/pgvector)
· R2(사진 저장) · Cloudflare Images(리사이징) · Google Maps · Vertex AI(Gemini)

---

## 확정된 결정 — 되돌리지 말 것

**아래는 이미 합의된 동작이다. 다른 작업을 하다가 원복하거나 없애지 말 것.**
바꿔야 할 이유가 생기면 먼저 물어본다.

- **KHUAC AI는 지도 화면 안에서 동작한다.** 별도 페이지로 이동시키지 않는다.
  (`src/app/assistant/page.tsx` 로 라우팅하는 변경은 이 규칙 위반이다.)
- AI는 **최근/장소별 앨범 전환 버튼에 종속되지 않는다.** 앨범 전환 위에 둔다.
- 답변 본문과 "지도에서 보기" 박스는 **하나로 합쳐서** 보여준다.
- "지도에서 보기"에 **출발–도착 문구를 넣지 않는다.**
- 지도 위 **간편조작 방향키는 없앴다.** 다시 넣지 않는다.
- 마커 클릭 시 사진첩을 **전체화면으로 열지 않는다.** 왼쪽에 미니맵을 두고
  해당 코스를 라인으로 표시한다. (실내/실외 암장 제외)
- 마커의 한글 라벨은 **줌 아웃 상태에서만** 보이고 줌인하면 숨긴다.
- 앨범/코스 화면에서 **"지도에서 보기"와 "앨범 추가" 버튼은 유지한다.**
- 사진 GPS만으로 코스 라인을 자동 생성하지 않는다. 사진은 찍힌 위치에
  미리보기로 띄운다.
- **코스는 출처 순으로 믿는다: `gpx` > `knps` > `club` > `forest` > `search`.**
  실제로 걸은 트랙이 측량을 이기고, 측량이 부원의 기억을 이기고, 셋 다
  웹 문장을 이긴다. `course_library.origin` 에 들어 있고
  `ORIGIN_RANK`(`src/app/assistant/actions.ts`)가 판정한다.
  - 검색 결과가 더 나은 출처의 행을 **덮어쓰지 않는다.** `rememberCourses`
    가 upsert 전에 기존 `origin` 을 읽어 걸러낸다.
  - 그래도 `search` 는 계속 저장한다. 불암산·수락산처럼 **아무것도 없는
    산에서는 검증 안 된 답이라도 없는 것보다 낫다.** 나중에 더 나은 출처가
    덮을 자리로 둔다.
  - `forest`(산림청)는 측량이 아니라 **설명문에서 뽑은 것**이라 `knps` 아래다.
  - **새 출처를 만들면 `ORIGIN_RANK` 에 반드시 넣는다.** 빠지면 최하위로 떨어져
    검색 결과에 덮인다 — `forest` 502행이 실제로 그렇게 들어갔었다.
  - 앞으로 GPX를 함께 받는다. 누적되면 그 트랙으로 갱신한다.

---

## 완료 보고 전에 반드시

"고쳤습니다" 라고 말하기 전에 **직접 확인한다.** 이 프로젝트에서 가장 자주
반복된 문제가 "고쳤다고 했는데 여전히 안 되는 것" 이다.

```bash
npm run lint
npx tsc --noEmit
npm test
```

세 개가 통과했다고 고쳐진 것도 아니다. UI 변경이면 `khuac-run`
(→ `npm run cf:preview`) 으로 띄워 **실제 화면에서** 확인한다. 코드를 읽고
"될 것 같다" 로 끝내지 않는다. 확인하지 못한 부분은 확인하지 못했다고 밝힌다.

- 미리보기는 **http://localhost:3100** 으로 연다. `127.0.0.1:3100` 으로 열면
  Google Maps 키가 거부한다.
- 3100 포트가 이미 쓰이고 있으면 기존 서버를 끄고 다시 띄운다.
- dev 서버가 떠 있으면 `cf:build` 가 `.open-next` 잠금으로 실패한다. 빌드 전에
  먼저 끈다.
- **배포는 로컬에서 하지 않는다.** GitHub push → 테스트 → 배포 순서를 지킨다.

---

## 증상이 아니라 원인을 고친다

- **예외 목록을 늘려 증상을 덮지 않는다.** "◯◯입구면 제외" 같은 규칙은 오류를
  줄이는 게 아니라 늘린다. 왜 그 입력이 잘못 처리되는지 밝힌다.
- 같은 오류가 두 번 이상 나오면 **패치를 멈추고 원인을 분석한다.**
- 문자열 길이·접두사 같은 **임시 휴리스틱**(예: "세 글자 일치") 대신 실제
  데이터를 비교한다. 코스 판별은 이름이 아니라 **경로 유사도**로 한다.

---

## 도메인 용어 — 틀리면 기능이 틀린다

| 용어 | 뜻 |
| --- | --- |
| 등산 | 일반 산행. 등산로/트레일. |
| 등반 | 보통 암벽등반. 어프로치·퀵드로·개념도가 따라온다. |

- 질문에 **어프로치 · 등반 · 암벽** 같은 키워드가 있으면 산 태그가 아니라
  **외벽 / 실내암장 / 암벽등반** 중에서 태그를 붙인다.
- **암벽등반 답변에는** 준비물(퀵 개수) · 개념도 · 어프로치 루트가 들어간다.
- **장소명은 한글로 저장한다.** Google Maps 검색 결과가 영어로 들어오면 고친다.
  (Gwanaksan ✗ → 관악산 ✓)
- 위치가 특정되지 않은 요청은 **서울 기준**으로 처리한다. "경희대 근처" 는
  캠퍼스가 둘이므로 서울캠 기준으로 답하거나 둘 다 제시한다.

---

## AI 어시스턴트 답변 규칙

- 마크다운 `#` 과 `*` 를 쓰지 않는다. 글씨체·크기로 위계를 표현한다.
- 인사말·서론 없이 질문에 바로 답한다. 핵심을 먼저, 자세히.
- 답변에 Google Maps 링크나 지도 이미지를 첨부하지 않는다. 왼쪽 지도와
  연동해서 폴리라인으로 보여준다.
- **최신 통제 정보는 반드시 검색하고 답변 맨 위에 둔다.**
- 검색 중 문구는 `DB에 없으니 웹 검색 중입니다` 하나만 쓴다. "답변을
  받았지만~" 같은 문구는 쓰지 않는다.
- 이미 조회한 정보는 반복 호출하지 않는다. 최근 검색 기록에서 재사용한다.
- 답변 형식은 항상 같은 구조로 통일한다.

---

## 데이터 일관성

- **삭제는 연쇄 반영되어야 한다.** 앨범을 지우면 마커·폴리라인·연결된 사진이
  같이 정리된다. 한쪽만 지우고 끝내지 않는다.
- 상위 카테고리 앨범은 하위 폴더가 없어도 앨범 목록에 보여야 한다.
- 한 장소에 코스가 여럿이면 **전부** 보여준다. 먼저 등록된 것만 보이면 버그다.
- 같은 코스가 이름만 다르게 중복 등록되지 않게 한다. 폴리라인이 겹치면 합친다.
- 경유지 마커는 **실제 경유지 위치에 하나씩** 단다. 한 점에 몰아 찍지 않는다.

---

## 비용

**비용과 예상 사용량을 항상 계산한다.** 기능을 제안할 때 호출 횟수와 단가를
같이 적는다.

- Gemini·Places 호출은 건당 과금된다. 새 기능을 넣을 때 예상 호출량과 비용을
  먼저 계산하고, 캐시·호출 상한을 함께 설계한다. 위치·날씨처럼 우리 DB와 무료
  API로 답할 수 있는 질문은 모델을 태우지 않는다.
- Google Maps 키: HTTP 리퍼러 제한(khuac.com, localhost), 일일 할당량 제한,
  결제 알림을 반드시 설정한다.
- 캐시를 늘릴 때 비용이 어떻게 변하는지 먼저 따진다.
- 사진은 계속 누적된다. 저장·변환 비용이 선형으로 늘어나는 설계를 피한다.

---

## 작업 방식

- 서로 **독립적인 작업이 여러 개면 subagent 를 써서 병렬로** 진행한다.
- 지도·코스처럼 **눈으로 봐야 아는 것은** 스크린샷이나 실제 화면으로
  확인받는다.
- 큰 변경 전에 기존 동작이 깨지지 않는지 위 **"확정된 결정"** 목록을 먼저
  읽는다.

### 턴 중간에 들어온 요청

작업 중에 새 요청이 들어오면 **하던 작업을 멈추지 말고 계속 진행하면서**, 새 요청은
**서브에이전트를 열어 병렬로** 처리한다. 하던 일을 중단하고 새 요청으로 갈아타지 않는다.

**왜:** 진행 중이던 작업을 중간에 끊으면 반쯤 된 상태로 남아 나중에 다시 문맥을 복구해야
한다. 두 요청이 서로 다른 파일을 건드리는 경우가 대부분이라 병렬로 하는 편이 빠르다.

**적용:** 같은 파일을 양쪽에서 고쳐야 하는 경우에만 예외로, 순서를 정해 순차 진행하고 그
이유를 밝힌다. 서브에이전트에는 배경·목표·이미 확인된 사실을 함께 넘긴다 — 서브에이전트는
이 대화를 보지 못한다.

---

## 배포

`wrangler deploy`가 성공했다고 반영된 것이 아니다. 업로드만 되고 승격되지 않아 엣지가 옛
버전을 계속 실행한 사례가 있었다. 배포 후 `npx wrangler deployments status`로 실제 서빙
버전을 확인하고, 자산 전파에 1분 정도 걸리는 것을 감안한다.

dev 서버가 떠 있으면 `npm run cf:build`가 실패한다 — dev의 workerd 자식 프로세스가
`.open-next`를 잠근다. 빌드·배포 예정이면 dev 서버를 띄우지 않는다.

## 비밀값

API 키와 서비스 계정 JSON은 대화에 붙여넣지 않는다. `.env.local`에 넣고 파일 경로로만
주고받으며, 워커 시크릿 등록은 파일 기반 `wrangler secret bulk`를 쓴다 —
`printf | wrangler secret put` 방식은 이 환경에서 값을 깨뜨린 적이 있다.

---

## 구조

```
src/app/             라우트 (map · assistant · photos · admin · hikes · members)
src/lib/assistant/   AI 어시스턴트 (intent · resolve · routes · cache · markdown)
src/lib/gps/         GPS·트랙·사진 매칭 (haversine · track · photo-groups · exif)
src/lib/routes/      등산로 스냅·타일·고도 (snap · tiles · trails · poi · elevation)
src/lib/gemini/      Vertex AI 클라이언트
src/lib/r2/          R2 스토리지
src/lib/images/      Cloudflare Images URL
supabase/migrations/ 스키마
scripts/             일회성·점검용 스크립트 (측정·백필·로그 관찰)
```
