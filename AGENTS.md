# 변경 검토와 배포

- 사용자가 확인하기 전에는 수정 내용을 커밋하거나 push하지 않는다.
- 작업은 로컬 작업 브랜치에서 진행한다. main 직접 push 및 운영 배포는 사용자가 검토 후 명시적으로 요청할 때만 수행한다.
- 사용자 확인용 미리보기는 `npm run cf:preview`로 실행하는 `http://localhost:3100`을 사용한다. `next dev`나 `next start`를 운영 검증으로 대체하지 않는다.
- 미리보기는 운영과 같은 OpenNext 빌드, workerd, wrangler.jsonc의 compatibility date/flags를 사용한다. 운영 엣지 네트워크와 외부 서비스까지 동일하다고 주장하지 않는다.
- 재빌드 전에 기존 3100 미리보기를 종료한다. 다른 프로젝트의 프로세스는 종료하지 않는다.
- 로컬 R2 바인딩 캐시는 운영과 분리되지만 `.env.local`의 Supabase 및 사진용 S3 연결은 실제 서비스일 수 있다. 미리보기만으로 데이터까지 격리되었다고 설명하지 않는다.
- 검증 후 변경 사항, 실행 주소, 테스트 결과를 전달하고 사용자 확인을 기다린다.
